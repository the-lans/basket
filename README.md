# Баскет — 1v1 Half-Court Basketball Game

Изометрическая баскетбольная игра 1 на 1 для браузера. Vanilla JavaScript, HTML5 Canvas, современный CSS3.

## 🎮 Особенности

- **Изометрическая камера** — псевдо-3D вид на половину корта
- **Реалистичная физика мяча** — баллистика, отскоки от щита и пола
- **Умный AI** — три уровня сложности с различными стратегиями
- **Система правил стритбола** — игра до 11 очков, check ball, очистка мяча за дугой
- **Настраиваемый геймплей** — шот-клок 12/24/∞, системы очков 1/2 или 2/3
- **Современный UI** — glassmorphism стиль, неоновые эффекты, адаптивный дизайн

## 🚀 Запуск

### Требования
- Современный браузер с поддержкой ES2020 модулей
- Node.js (для dev-сервера, опционально)

### Быстрый старт

```bash
# Клонировать репозиторий
git clone <repo-url>
cd basket

# Установить зависимости (опционально, для dev-инструментов)
npm install

# Запустить dev-сервер
npm run dev

# Открыть в браузере
# http://localhost:3000
```

### Альтернатива без Node.js

Откройте `index.html` через любой локальный веб-сервер:

```bash
# Python 3
python -m http.server 3000

# PHP
php -S localhost:3000
```

## 🎯 Управление

| Клавиша | Действие |
|---------|----------|
| **WASD** или **Стрелки** | Движение игрока |
| **Shift** | Спринт (расходует выносливость) |
| **ЛКМ (удерживать)** | Зарядка броска, отпустить для выполнения |
| **ПКМ** | В атаке: финт/step-back, в защите: попытка перехвата |
| **Esc** | Пауза |

## 📐 Архитектура

```
basket/
├── index.html              # Главная страница
├── styles/                 # CSS стили
│   ├── base.css           # Базовые стили и переменные
│   ├── ui.css             # UI компоненты
│   └── animations.css     # CSS анимации
├── src/
│   ├── main.js            # Точка входа
│   ├── config.js          # Конфигурация игры
│   ├── core/              # Ядро игры
│   │   ├── Game.js        # Главный класс игры
│   │   ├── Loop.js        # Игровой цикл (fixed timestep)
│   │   ├── SceneManager.js
│   │   ├── InputManager.js
│   │   ├── AudioManager.js
│   │   ├── Math.js        # Математические утилиты
│   │   └── Projection.js  # Изометрическая проекция
│   ├── scenes/            # Игровые сцены
│   │   ├── BootScene.js
│   │   ├── PreloadScene.js
│   │   ├── MenuScene.js
│   │   ├── GameScene.js
│   │   ├── PauseScene.js
│   │   └── GameOverScene.js
│   └── game/              # Игровая логика
│       ├── Player.js      # Игрок
│       ├── Ball.js        # Мяч с физикой
│       ├── AIController.js # AI с состояниями
│       ├── Physics.js     # Физика и коллизии
│       ├── CourtRenderer.js # Отрисовка корта
│       ├── Rules.js       # Правила игры
│       └── HUD.js         # Интерфейс
├── design/                # Дизайн-референсы
└── package.json           # NPM конфигурация
```

## ⚙️ Настройка параметров

Все параметры игры находятся в `src/config.js`:

```javascript
// Скорость игрока
PLAYER: {
    BASE_SPEED: 3.6,           // м/с
    SPRINT_MULTIPLIER: 1.5,    // Множитель спринта
    STAMINA_MAX: 100,          // Максимальная выносливость
}

// Параметры броска
BALL: {
    MAGNETIC_WINDOW: 0.08,     // "Магнитное" окно попаданий (метры)
    SHOT_BASE_SPEED: 6.0,      // Базовая скорость броска
    SHOT_DISTANCE_FACTOR: 0.5, // Коэффициент дистанции
}

// Шот-клок
GAME: {
    SHOT_CLOCK_DEFAULT: 24,    // Секунды
    WIN_SCORE: 11,             // Очки для победы
}

// Сложность AI
AI: {
    DIFFICULTIES: {
        STANDARD: {
            SHOT_ACCURACY: 0.75,
            DEFENSE_DISTANCE: 1.2,
            STEAL_CHANCE: 0.25,
        }
    }
}
```

## 🎨 Цветовая схема

Основана на дизайне из `design/design1.png`:

- **Корт**: `#F3D24B` (желтый паркет)
- **Трибуна слева**: `#FF4D5A` (красная, команда AI)
- **Трибуна справа**: `#39B8FF` (голубая, команда игрока)
- **Неоновые акценты**: `#7CFFEA` (cyan)
- **Разметка**: `#FFFFFF` (белая с свечением)

## 🧪 Тестирование

```bash
# Линтинг
npm run lint

# Форматирование
npm run format

# Проверка типов (JSDoc)
npm run typecheck
```

## 📝 Правила игры

1. **Счет**: Первый до 11 очков побеждает
2. **Очки**: 1 очко внутри дуги, 2 очка за дугой (стритбольная система по умолчанию)
3. **Check ball**: После каждого попадания мяч возвращается к линии штрафной
4. **Очистка мяча**: После смены владения нужно вывести мяч за трехочковую линию
5. **Шот-клок**: 24 секунды на атаку (настраивается)
6. **Фолы**: При агрессивном контакте — 1 штрафной бросок

## 🔧 Технологии

- **Vanilla JavaScript ES2020+** — без фреймворков
- **HTML5 Canvas 2D** — отрисовка игры
- **Web Audio API** — звуковые эффекты
- **CSS3** — градиенты, анимации, glassmorphism
- **ES Modules** — нативная модульность
- **JSDoc + TypeScript** — типизация без транспиляции

## 📊 Производительность

- **Целевой FPS**: 60
- **Fixed timestep**: 1/60 с интерполяцией рендера
- **Оптимизации**: offscreen canvas для статических элементов, пулы объектов
- **Размер**: ~100-150 KB gzip (без ассетов)

## 🎯 Roadmap

- [ ] Звуковые эффекты (свист, отскок мяча, swish)
- [ ] Фоновая музыка в меню
- [ ] Улучшенные анимации игроков (skeleton animation)
- [ ] Эффекты частиц (пот, пыль от паркета)
- [ ] Локальная таблица рекордов
- [ ] Больше архетипов игроков
- [ ] Мультиплеер (WebRTC peer-to-peer)

## 📄 Лицензия

MIT

## 🤝 Контрибуция

Pull requests приветствуются! Для больших изменений откройте issue для обсуждения.

---

**Приятной игры! 🏀**



